import cachetools
import random
import hashlib
from .gigachat_api import gigachat_request

# Кэш на 1 час для одинаковых запросов
cache = cachetools.TTLCache(maxsize=1000, ttl=3600)

# Фолбэки — генерируют промпты для ИИ
FALLBACK_PROMPTS = {
    'question': [
        "Создай идеальный промпт для ИИ, который даст глубокий, структурированный ответ на вопрос: {input}. Включи запрос на примеры, контраргументы и источники.",
        "Сформулируй профессиональный запрос для ИИ, чтобы получить максимально полный анализ вопроса: {input}. Запрос должен включать разделы: контекст, аргументы, вывод.",
        "Напиши промпт для ИИ, который превратит вопрос '{input}' в эссе с введением, телом и заключением."
    ],
    'event': [
        "Создай промпт для ИИ, чтобы он проанализировал событие '{input}' с точки зрения причин, последствий, участников и 3 альтернативных исходов.",
        "Сформулируй запрос для ИИ, чтобы он исследовал событие '{input}' и дал прогнозы на будущее, основанные на исторических аналогах.",
        "Напиши промпт, который заставит ИИ сравнить событие '{input}' с аналогичными в истории и выделить уникальные черты."
    ],
    'advice': [
        "Создай пошаговый промпт для ИИ, чтобы получить практический совет по '{input}'. Включи запрос на конкретные действия, сроки, ресурсы и возможные ошибки.",
        "Сформулируй запрос для ИИ, чтобы он разбил решение '{input}' на этапы: подготовка, реализация, оценка.",
        "Напиши промпт, который заставит ИИ предложить 3 разных подхода к '{input}' с плюсами, минусами и рекомендациями по применению."
    ],
    'book': [
        "Ты — интеллектуальный повествователь, мастер психологического портрета через художественный текст. Твоя задача — провести короткий, ненавязчивый опрос (5–7 вопросов), чтобы понять личность пользователя. После получения ответов создай уникальный художественный рассказ (300–500 слов)."
    ],
    'bot': [
        "Создай детальный промпт для ИИ, который поможет пользователю создать Telegram-бота с нуля на Python. Включи пошаговые инструкции по настройке, кодингу и деплою.",
        "Сформулируй исчерпывающий запрос для ИИ по созданию Telegram-бота, охватывающий установку библиотек, базовую структуру и примеры функциональности.",
        "Напиши промпт, который заставит ИИ стать личным преподавателем по созданию Telegram-бота для пользователя с любым уровнем подготовки."
    ],
    'image': [
        "Сформируй промпт изображения для: {input}. Укажи сюжет, стиль (например, cinematic, analog film), композицию (rule of thirds), освещение (soft light), ракурс (close-up/medium/wide), объектив (35mm/50mm), цветовую палитру, ключевые детали, фон и пост-обработку."
    ],
    'video': [
        "Сформируй сториборд-промпт для ролика: {input}. Укажи логлайн, длительность, 5–8 сцен с тайм-кодами, короткое описание каждого кадра (ракурс/план/движение камеры), атмосферу, звуки/музыку, ключевой визуальный стиль."
    ],
}

async def generate_enhanced_prompt(category: str, user_input: str, **kwargs) -> str:
    # Системные промпты
    base_system_prompts = {
        'question': "Ты — эксперт по созданию идеальных промптов для ИИ. Создай профессиональный, детализированный запрос, который заставит ИИ дать максимально глубокий и структурированный ответ.",
        'event': "Ты — мастер создания аналитических промптов. Сформулируй запрос, который заставит ИИ провести детальный анализ события с причинами, последствиями и прогнозами.",
        'advice': "Ты — специалист по практическим промптам. Создай запрос, который заставит ИИ дать пошаговый, конкретный совет с инструментами и сроками.",
        'book': "Ты — создатель промптов для художественных ИИ. Верни идеальный системный промпт для сценария «Книга о себе». Только промпт, без пояснений.",
        'bot': "Ты — эксперт по обучающим промптам для разработки. Верни детальную инструкцию, которая поможет пользователю создать Telegram-бота с нуля. Только промпт, без пояснений.",
        'image': "Ты — инженер промптов по генерации изображений. Верни один идеальный промпт с: сюжетом, стилем, ракурсом, композицией, освещением, ключевыми деталями. Никаких пояснений.",
        'video': "Ты — инженер промптов для генерации видео/сторибордов. Верни структуру: логлайн, сцены с тайм-кодами, описание плана/кадра, движение камеры, текст-озвучка (если нужно), визуальные референсы. Никаких пояснений."
    }
    system_prompt = base_system_prompts.get(category, "Создай идеальный промпт для ИИ.")

    cache_key = f"{category}:{hashlib.md5(system_prompt.encode()).hexdigest()}:{hashlib.md5(user_input.encode()).hexdigest()}"
    if cache_key in cache:
        print(f"📦 [Кэш] Возвращаем закэшированный промпт для: {user_input[:30]}...")
        return cache[cache_key]

    try:
        # Для 'book' и 'bot' — отдаем заранее заготовленные промпты из texts
        if category == "book":
            from texts import BOOK_START_PROMPT
            result = BOOK_START_PROMPT
        elif category == "bot":
            from texts import BOT_CREATION_PROMPT
            result = BOT_CREATION_PROMPT
        else:
            messages = [
                {"role": "system", "content": system_prompt},
                {"role": "user", "content": user_input}
            ]

            params = {
                'question': {'temperature': 0.3, 'max_tokens': 300},
                'event':    {'temperature': 0.4, 'max_tokens': 350},
                'advice':   {'temperature': 0.5, 'max_tokens': 400},
                'book':     {'temperature': 0.1, 'max_tokens': 100},
                'bot':      {'temperature': 0.4, 'max_tokens': 350},
                'image':    {'temperature': 0.4, 'max_tokens': 250},
                'video':    {'temperature': 0.4, 'max_tokens': 350}
            }.get(category, {'temperature': 0.7, 'max_tokens': 200})

            result = await gigachat_request(
                messages=messages,
                temperature=params['temperature'],
                max_tokens=params['max_tokens']
            )

        if len(result.strip()) < 10:
            raise ValueError("Слишком короткий промпт")

        cache[cache_key] = result
        print(f"🧠 [GigaChat] Новый промпт сгенерирован и закэширован для категории: {category}")
        return result

    except Exception as e:
        error_msg = f"⚠️ Ошибка генерации промпта: {type(e).__name__} - {str(e)[:150]}"
        print(error_msg)
        fallbacks = FALLBACK_PROMPTS.get(category, FALLBACK_PROMPTS['question'])
        if '{input}' in fallbacks[0]:
            chosen = random.choice(fallbacks).format(input=user_input)
        else:
            chosen = fallbacks[0]
        print(f"🔄 Используем fallback: {chosen[:80]}...")
        return chosen
